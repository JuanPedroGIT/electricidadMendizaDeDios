framework:
    rate_limiter:
        contact_form:
            policy: 'sliding_window'
            limit: 5
            interval: '10 minutes'
            cache_pool: 'rate_limiter.cache'
        login:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
            cache_pool: 'rate_limiter.cache'
